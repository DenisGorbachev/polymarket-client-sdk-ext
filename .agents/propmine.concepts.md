# Propmine concepts

## Property

An `impl Fn(&T) -> bool`.

Synonyms: prop.

## PropertyFn

An `fn(&T) -> bool`.

## Propmine

A Rust fn with the following properties:

* Inputs:
  * `impl Iterator<Item = &T>`
  * `Vec<>`
