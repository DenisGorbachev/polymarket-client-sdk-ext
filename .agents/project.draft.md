* Requirements:
  * Must have the round-trip tests for every [extension type](project.md#extension-type) that asserts that every `polymarket_client_sdk::clob::types::response::MarketResponse` can be round-tripped into `Market`
